---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.6.1
  creationTimestamp: null
  name: backends.solution.feature.aero.sample.com
spec:
  group: solution.feature.aero.sample.com
  names:
    kind: {{ .Values.legacy.kind }}
    listKind: {{ .Values.legacy.listKind }}
    plural: {{ .Values.legacy.plural }}
    singular: {{ .Values.legacy.singular }}
  scope: Namespaced
  versions:
    - name: {{ .Values.legacy.version }}
      schema:
        openAPIV3Schema:
          description: Backend is the Schema for the backends API
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
              type: string
            metadata:
              type: object
            spec:
              description: BackendSpec defines the desired state of Backend
              properties:
                backendApps:
                  description: BackendApps lists all the applications to be deployed.
                  items:
                    description: BackendApp defines the structure of the backend object
                    properties:
                      image:
                        type: string
                      name:
                        type: string
                      parameters:
                        additionalProperties:
                          type: string
                        type: object
                      port:
                        type: integer
                      version:
                        type: string
                    type: object
                  type: array
              type: object
            status:
              description: BackendStatus defines the observed state of Backend
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: { }
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: [ ]
  storedVersions: [ ]
